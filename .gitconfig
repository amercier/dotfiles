[alias]
	amend = commit --amend --no-edit
	clean-merged-branches = ! git merged-branches | xargs git branch -d
	cleanup = ! git checkout master && git pull && git clean-merged-branches
	clone-branch = clone -b
	commit-message = "!git_commit_message() { message="$@"; git commit -m \"$message\"; }; git_commit_message"
	current = symbolic-ref --short HEAD
	diff-cached = diff --cached
	graph = log --graph --pretty=friendly --color=always
	merged-branches = ! git branch --merged | egrep -v '^\\*'
	push-all = ! source $HOME/.dotfiles/profile.d/git && git_push_all
	push-upstream = "! git push --set-upstream \"$(git remote | head -n 1)\" \"$(git current)\""
	show-files = show --name-only
[color "advice"]
	hint = black
[color "branch"]
	local = yellow
	remote = blue
	plain = magenta
	upstream = blue bold
[color "decorate"]
	branch = yellow
	HEAD = yellow dim
	remoteBranch = blue
	tag = magenta
[color "diff"]
	commit = blue
	context = normal
	frag = black
	func = black
	meta = blue
	whitespace = red reverse
[commit]
	gpgsign = true
[core]
	excludesfile = /Users/amercier/.dotfiles/extra/.gitignore_global
	pager = less -x1,5
[pretty]
	friendly = "\t%C(black)%h%C(reset) %<(44,trunc)%s %C(cyan)%>(10,trunc)%cr %C(dim white)%<(8,trunc)%aN%C(reset) %C(reset)%C(auto)%d%C(reset)"
[user]
	name = Alex Mercier
	email = pro.alexandre.mercier@gmail.com
	signingkey = A4A0BA9DE4FC1F8D
