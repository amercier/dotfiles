if which brew >/dev/null 2>&1 && brew --prefix docker-machine >/dev/null 2>&1; then
  name=$(docker-machine ls | head -n 2 | tail -n 1 | awk '{ print $1; }')
  if [ "$name" != "" ]; then
    eval $(docker-machine env "$name")
  fi
fi
