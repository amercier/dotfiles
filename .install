# Bash configuration

nvm --help

# Run all scripts for which the command doesn't exist and no function exist
# while read path; do
#   [ -x "$path" ] || continue
#   bin=$(basename "$path" | egrep -o '[^\.]+$')
#   if which "$bin" > /dev/null; then
#     echo "Binary $bin is already installed, skipping" >&2
#   elif [ "$(type -t "$bin")" == "function" ]; then
#     echo "Function $bin is already installed, skipping" >&2
#   else
#     t=$(type -t $bin)
#     declare -f "$bin" && echo ok || echo nok
#     declare -f -F "$bin" && echo ok || echo nok
#     echo "Running $path ($bin) => $(type -t $bin) $t"
#     echo "PATH = $PATH"
#     $bin --help
#     # "$path"
#   fi
# done < <(find "$HOME/.install.d" -type f)
